import { EmailNormalizeDirective } from './email-normalize.directive';

describe('EmailNormalizeDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailNormalizeDirective();
    expect(directive).toBeTruthy();
  });
});
